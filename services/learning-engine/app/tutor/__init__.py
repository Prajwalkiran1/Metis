"""Tutor RAG pipeline (M7c).

When M7 lands, this package exposes ``ask(prompt, user_id, course_id)``
which performs RAG retrieval against the Qdrant collection and feeds
context to the LLM orchestrator.
"""
